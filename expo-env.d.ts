/// <reference types="expo/types" />

// Committed intentionally so `tsc` type-checks out of the box (e.g. in CI)
// without first booting the Expo CLI. Expo regenerates this file with identical
// content on `expo start` / prebuild, so it stays in sync.
